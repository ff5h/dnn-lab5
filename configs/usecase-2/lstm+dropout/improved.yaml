project: dnn-lab5
usecase: usecase-2
arch: lstm+dropout

config:
  max_len: 500
  max_words: 5000
  embedding_dim: 64
  lstm_units: 128
  dropout_rate: 0.3
  recurrent_dropout: 0.3
  batch_size: 128
  epochs: 10
  optimizer: adam
  loss: binary_crossentropy